@inject SignInManager<IdentityUser> signInManager;
@{
    ViewData["Title"] = "Home Page";
}

<div class="container-fluid home" >
    @*<div>
        <nav class="navbar navbar-expand-lg bg-primary navbar-dark homeNav">
            @if(signInManager.IsSignedIn(User))
            {
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" asp-action="Index">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-action="GetBadges">Badge Queue</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-action="BadgeOutPage">Badge Out</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-action="BadgeReportPage">Badge Report</a>
                    </li>
                </ul>
            }
            else
            {
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" asp-action="Index">Home</a>
                    </li>
                </ul>
            }
            
        </nav>
    </div>*@
    <div class="Sign">
        <div class="SignIn">
            <a asp-action="SignIn" class="btn btn-primary btn-lg">Assign</a>
        </div>
        <div class="SignOut">
            <a asp-action="SignOutPage" class="btn btn-primary btn-lg">Sign Out</a>
        </div>
    </div>
</div>
